version: "3.9"

services:
  fastapi-app:
    build: .
    container_name: fastapi-app
    ports:
      - "8000:80"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/postgres
      REDIS_HOST: redis
      REDIS_PORT: 6379
      
      MAIL_USERNAME: rafiuddin.tarif@gmail.com
      MAIL_PASSWORD: aaecnhfmooqtkvpg
      MAIL_FROM: rafiuddin.tarif@gmail.com
      MAIL_PORT: 587
      MAIL_SERVER: smtp.gmail.com
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      SECRET_KEY: 53b02889003befd34863537a9cbd4871ddf42c3a55ee7b766eb237ca35a14a7b
      ALGORITHM: HS256
    depends_on:
      - db
      - redis

  db:
    image: postgres:16
    container_name: postgres-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data

  redis:
    image: redis/redis-stack:latest
    container_name: redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data: